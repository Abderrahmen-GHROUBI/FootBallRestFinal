spring.application.name=FootballCompetition
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/${DB_NAME:SportCompetition}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:x0we80/h}

# Configurations additionnelles pour MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
server.port=9090
